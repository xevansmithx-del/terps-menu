name: E2E Tests

on:
  workflow_dispatch:
  # NOTE: nightly schedule removed 2026-07-02 — tests create real orders in the
  # production queue; re-enable only after guaranteed afterAll cleanup + retries:0.

jobs:
  e2e-consumer:
    name: Consumer order flow
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: e2e
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: e2e/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run consumer e2e tests
        run: npx playwright test tests/consumer.spec.ts
        env:
          BASE_URL: https://terpsdispensary.com

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: e2e/test-results/
          retention-days: 7
