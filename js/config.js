/* ============================================================
   TERPS storefront — backend config
   Real values are injected by the deploy coordinator (search/replace
   the __PLACEHOLDER__ tokens). While the placeholders are unchanged,
   orders.js runs in local-preview DEMO mode (localStorage only).
   ============================================================ */
window.TERPS_BACKEND = {
  supabaseUrl: "https://ckwttfkiaisimkrrsfaf.supabase.co",
  supabaseAnonKey: "sb_publishable_NxFajU8w0usJxbqiuG00-w_6uqqHfQB",
  siteUrl: "https://shop.terpsdispensary.com"
};
